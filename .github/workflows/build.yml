---
name: Run compile of programs
on: [ push ]
jobs:
  build:
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v2
      with:
        go-version: '^1.16.1' # The Go version to download (if necessary) and use.
    - run: ./compile.sh